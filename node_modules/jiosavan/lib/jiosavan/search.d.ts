import { RSearch, Song, Playlist, Artist } from '../interfaces/Search';
declare const _default: {
    searchSongs: (query: string) => Promise<Song[]>;
    searchAlbums: (query: string) => Promise<Song[]>;
    searchArtist: (query: string) => Promise<Artist[]>;
    searchPlaylists: (query: string) => Promise<Playlist[]>;
    search: (query: string) => Promise<RSearch>;
};
export default _default;
